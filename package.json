{
  "name": "asset_pull",
  "version": "1.0.0",
  "description": "Download CSS, JS, and images from a live URL",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Scott Rothman",
  "license": "ISC",
  "dependencies": {
    "cheerio": ">= 0.19.0",
    "prompt": ">= 0.2.14",
    "request": ">=2.55.0"
  }
}
